# Revisit Plan — M002

M003 replaces repeated recursion with folds under strictness pressure; the typeclass phase evaluates Foldable/Functor instances; the text-pipeline boss requires streaming, chunk boundaries, and bounded residency. M004 independently transfers recursion to branching structure.
