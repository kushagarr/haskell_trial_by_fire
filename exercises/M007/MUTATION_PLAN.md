# Mutation Plan — M007

Wrong identity count; reverse label order; discard one side; use max instead of addition; deduplicate labels; summarize with quadratic left association; leave valueTotal lazy across a long chain. Laws/units kill semantic faults; scaling/heap evidence kills operational faults.
