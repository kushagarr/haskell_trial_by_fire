# Public Test Plan — M007

Units demonstrate identity, ordered combination, entry construction, and summary totals. Properties test associativity, left/right identity, summarize concatenation homomorphism, and output invariant. Public generators include arbitrary fields so law tests do not assume entryCount equals labels length.
