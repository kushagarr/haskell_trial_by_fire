# Mutation Plan — M003

Mutants: omit accumulator forcing; force the right-fold tail before applying the combiner; reverse map output; decrement take after rather than before boundary; consume one element for nonpositive take; implement strict sum through result list construction. Semantic properties, productivity categories, and RTS evidence divide responsibility.
