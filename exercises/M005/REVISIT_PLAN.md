# Revisit Plan — M005

B001 uses optional variable lookup but converts absence to a diagnostic at the boundary; M006 makes that distinction explicit; collections replace linear lookup when scale requires; Smithy resolution distinguishes optional traits from invalid references.
