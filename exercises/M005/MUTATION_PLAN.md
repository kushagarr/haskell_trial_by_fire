# Mutation Plan — M005

Return last match; treat empty lead as absent; default missing team ID to zero; search teams before user; perform the user lookup twice; ignore preferredTeam and choose first team; use fromJust. Public units/properties kill semantics; private counters/source review kill operational defects.
