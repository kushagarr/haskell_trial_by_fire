# Public Test Plan — M006

Units cover missing, valid boundaries, invalid syntax classes, out-of-range values, endpoint order, and path prepend. Properties cover round trips for valid ports and prepend composition. Very long digits, Unicode, and exact scaling are private categories mapped to FR-2/3 and performance clauses.
