# Revisit Plan — M006

B001 transfers structured parse/eval diagnostics; applicative validation changes fail-fast to accumulation; parser phase adds source spans and recovery; networking adds retry/error taxonomy and hostile payload limits.
