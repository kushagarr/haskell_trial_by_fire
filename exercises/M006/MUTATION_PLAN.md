# Mutation Plan — M006

Accept empty or sign; use read and overflow; accept zero/65536; discard original invalid text; append rather than prepend path; attach path only to one constructor; parse port before name; convert every error to one string constructor. Public structure checks and private boundaries kill these faults.
