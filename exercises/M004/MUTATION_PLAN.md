# Mutation Plan — M004

Omit one subtree; swap preorder/inorder; count Empty as one; take minimum child height; mirror only root; rebuild mapped tree with swapped children; compute traversal using repeated end append; recompute subtree sizes per node. Laws kill semantic faults; performance/deep categories kill operational faults.
