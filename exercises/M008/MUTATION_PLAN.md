# Mutation Plan — M008

Reverse identity, drop head/tail, reverse only prefix, duplicate elements; sort identity, descending sort, nub-after-sort, drop-minimum, fix only adjacent inversion, fail on empty. A strong property portfolio kills faults for the semantic reason it claims. Equivalent or irrelevant mutants are excluded rather than scored.
