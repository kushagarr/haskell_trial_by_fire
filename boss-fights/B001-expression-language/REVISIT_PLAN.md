# Revisit Plan — B001

| Trigger | New pressure/abstraction | Evidence |
|---|---|---|
| parser-combinator phase | factor repeated parser state/error logic | compare clarity, control, and diagnostics |
| document algebra | deterministic printer with layouts | round trips and output scaling |
| language phase | names, static types, typed AST/GADT | invalid programs rejected earlier |
| compiler phase | desugaring and IR passes | semantic preservation |
| incremental phase | chunks, spans, recovery | bounded input retention and partial state |

The expression domain recurs; the learner does not replace this attempt with a supplied solution.
