# Adversarial Block Review

First read root AGENTS.md in full. Enter CURRICULUM_AUTHOR review mode and do not implement fixes during review.

For each proposed module enumerate relevant empty, singleton, boundary, duplicate, malformed, deep, large, cyclic, disconnected, Unicode, partial-input, exception, cancellation, and race categories—explicitly marking domains that do not apply. Check every category is documented before any hidden plan tests it. Look for weak implementations likely to pass examples. Review realistic mutants and shrinking. Report missing or unfair categories separately from implementation suggestions.
